function Notify(type, title, msg, time)
    ESX.ShowNotification(msg)
end

Config = {
    jobscreator = {
        disableHandcuffed = true,
        tempCars = true,
        permCars = false, 
    },
    carlockCooldown = 2000,
}